# Database helpers

MongoDB connection helpers live in this folder. Copy `.env.example` to `.env.local` and set `MONGODB_URI` before importing `mongodb.ts` from a server component, route handler, or server action.
